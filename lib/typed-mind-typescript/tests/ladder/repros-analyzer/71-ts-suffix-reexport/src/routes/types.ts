// A `.ts`-suffixed relative re-export specifier, legal under
// `allowImportingTsExtensions` and idiomatic in Node type-stripping
// projects. The barrel names the source file with its real extension.
export { list_types_route } from './types-list.ts';
