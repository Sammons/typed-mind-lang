// Distilled signature of infra/api.ts from the real webhookstorage clone —
// the root-tsconfig-included entrypoint (compiled with the root
// `moduleResolution: bundler` config, which never sees `packages/`).
export const apiFn = {
  handler: 'packages/functions/src/api/index.handler',
};
