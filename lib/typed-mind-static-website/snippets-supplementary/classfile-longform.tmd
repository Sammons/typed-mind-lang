# Supplementary content for classfile-longform snippet
# The snippet's mainFile imports 'utilities', which needs a File entity to
# resolve against; the snippet does not define one so it lives here.

file utilities {
  path: "src/utils/methods.ts"
  exports: [findById, create, validate]
}
