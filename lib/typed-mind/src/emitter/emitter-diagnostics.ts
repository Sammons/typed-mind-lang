// Retained public identifiers for consumers that filter historical diagnostics.
// RFC-TM-13 C-prime makes both quote substitution and the nested quoted alias
// wrapper unnecessary. The emitter no longer produces these warnings.
export const QUOTE_SWAP_CODE = 'emitter/quote-swap';
export const UNREPRESENTABLE_ALIAS_CODE = 'emitter/unrepresentable-alias';
